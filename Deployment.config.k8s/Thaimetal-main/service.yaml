apiVersion: v1
kind: Service

metadata:
  name: prime-wms-erp-core
  namespace: prime-wms

  labels:
    app: prime-wms-erp-core

spec:

  type: ClusterIP

  selector:
    app: prime-wms-erp-core

  ports:
    - name: http
      protocol: TCP
      port: 9115
      targetPort: 9115